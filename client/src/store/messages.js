const state = {
  history: [
    {name: 'jon', data: 'ahsdald', colour: '#ffcced'},
    {name: 'jon', data: 'ahsdald', colour: '#ffcced'}
  ]
};

const getters = {};

const actions = {};

const mutations = {};

const client = {
  namespaced: true,
  state: state,
  getters: getters,
  actions: actions,
  mutations: mutations
};

export default client;
